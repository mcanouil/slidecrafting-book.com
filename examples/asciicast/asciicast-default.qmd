---
format: docx
---

## Default asciicast usage

```{r}
asciicast::init_knitr_engine()
```

```{asciicast}
library(tidymodels)
```

## New slide

```{r}

library(plotly)

plot(mpg ~ disp, mtcars)
```